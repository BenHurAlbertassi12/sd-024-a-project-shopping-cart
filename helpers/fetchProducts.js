const url = 'https://api.mercadolibre.com/sites/MLB/search?q=computador'

const fetchProducts = () => {
  // seu código aqui
  const fetchSimulator = (url) => {
    if (!url || url !== ValidURL)
      return Promise(new Error('Produto Invalido'))
  }

  return fetch = url;
};

if (typeof module !== 'undefined') {
  module.exports = {
    fetchProducts,
  };
}
